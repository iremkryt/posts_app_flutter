name: posts_app_flutter
description: Posts App with clean architecture

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  #Bloc for state management
  flutter_bloc: ^8.1.2
  #Service locator
  get_it: ^7.2.0
  #Remote api
  internet_connection_checker: ^1.0.0+1
  http: ^0.13.5
  #Local cache
  shared_preferences: ^2.0.18

  dartz: ^0.10.1
  equatable: ^2.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


